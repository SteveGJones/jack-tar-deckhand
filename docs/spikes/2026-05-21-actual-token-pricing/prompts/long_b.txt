Figure 1: System Architecture of Jack-Tar Deckhand Presentation Pipeline. A technical system diagram rendered in clean schematic illustration style, suitable for academic publication and conference proceedings. The figure demonstrates a hierarchical flow from top to bottom with the orchestrator plugin (jack-tar-deckhand) centrally positioned at the centre-top, receiving TalkBriefs as structured input from the left side. Below the orchestrator, four specialised engine plugins are arrayed horizontally in a balanced layout: jack-tar-ollama positioned left (labeled "Local Draft Renders via Ollama"), jack-tar-cloud at centre-left (labeled "Cloud Providers: Google, OpenAI, FAL, Recraft"), jack-tar-msft-smartart at centre-right (labeled "Editable SmartArt Layouts 1–29"), and jack-tar-custom-smartart positioned right (labeled "Data Visualisation: Mermaid, Vega, Matplotlib"). Each engine plugin is connected to the orchestrator with bidirectional arrows annotated with payload types (Image Generation Request, SmartArt Specification Request, rendered image outputs, graphic data). Above the orchestrator, a sixth plugin jack-tar-superpower-bridge is shown as a peer wrapper with bidirectional arrows indicating enrichment flow and metadata exchange. At the bottom of the diagram, an optional external tool "paperbanana" is shown in a dashed-border box labeled "Optional: Academic Figure CLI Tool" with conditional-invocation arrows showing academic figure request routing. The rightmost column shows the final output: a completed production-ready .pptx presentation deck. All connections between components are labeled with specific payload types, directions, and data formats. The colour palette uses professional restraint: dark navy for primary component boxes, light grey for backgrounds, light blue for information-flow connection lines, with one reserved accent colour for critical orchestrator-coordination pathways. All text uses clean sans-serif font family at 11-12pt or smaller for readability. The entire diagram fits within a single readable frame at conference projection size (16:9 aspect ratio). No photographic or realistic textural elements. Method section (below figure caption): The orchestrator coordinates four specialised rendering engines through a step-wise procedural pipeline including brand profile analysis, rendering strategy classification, and per-slide output selection. Data section (below method): All four engines are independently installable Claude Code marketplace plugins; paperbanana academic figure integration is optional with automatic fallback to Nano Banana Flash 1K when absent. Results section: This distributed architecture achieved production-quality presentation assembly in 18 minutes average per-deck with total cost under five dollars per complete presentation including iteration.